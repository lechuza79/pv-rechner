// Existing brand artwork, shared by editorial renderers.
// The mask is extracted unchanged from illustrations-motion/house/composite.svg
// (confirmed-base V16, distributed in the V19 integration package). Its colour
// is supplied by the theme; it is decoration and never encodes a quantity.
export const brandAssets = {
  splashMask: "/brand/splash-blue-mask.svg",
} as const;
