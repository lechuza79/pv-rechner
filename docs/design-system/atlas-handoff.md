# Atlas preview handoff — 2026-09-21

## Checkpoint and ownership

- Branch: `codex/kommunenseite-design`
- Working directory: `/Users/eule/.codex/worktrees/8baa/pv-rechner`
- Preview: `http://localhost:4196/atlas-design-preview/index.html?displayfont=montserrat-bold#atlas-data`
- This is a local design/integration checkpoint, not a release. Do not deploy the preview adapter as a production service.
- Keep the worktree and running preview available. Other worktrees belong to other sessions; do not remove them.

## Resume / integrate

1. Read this file, `scripts/municipality-preview/README.md`, and `docs/design-system/dashboard-conventions.md`. This handoff supersedes older ranking handoff descriptions where they conflict.
2. Inspect the actual preview before changing its design. The user wants the accepted visuals preserved, not another reconstruction.
3. Start, only if port 4196 is free: `node --import tsx scripts/municipality-preview/server.ts` from the working directory. Dependencies are currently provided by a local `node_modules` symlink, intentionally not committed.
4. The adapter reads the main checkout (`/Users/eule/projects/pv-rechner`), the story checkout (`/Users/eule/projects/pv-rechner/.worktrees/codex-kommunen-templates`), and the shared hero handoff. See `server.ts` for `SOLAR_SITE_ROOT`, `STORY_SOURCE_ROOT`, and `SOLAR_SHARED_ROOT`. It is not yet portable to a clean deployment checkout.
5. Integrate the accepted composition/components with production routes and shared data loaders; run repository checks and inspect desktop/mobile rendering before release. No release or push has been performed by this checkpoint.

## Binary assets

The user's Git convention excludes binaries. The 64 local fonts and raster assets are listed with SHA-256 hashes in `scripts/municipality-preview/handoff-assets.json` and archived at:

`/Users/eule/.codex/worktrees/8baa/pv-rechner/scripts/municipality-preview/build/handoff-assets.tar.gz`

The archive is local, not committed. Restore from the repository root with `tar -xzf <archive-path>`. Original files remain in this worktree. Transfer this archive along with the commit if moving to another machine. Compiled preview bundles are also excluded and regenerated by the server. Public hero bundles and textual SVG assets are included.

## Current ranking behavior

- All 30 choices are visible from the start, with mystery placeholders until revealed.
- Titles start uppercase on both sides. Header: `Top 3 von X`; compact title, qualifier, comparison scope.
- Stable stage height, one vertically scrolling right column. Arrow navigation scrolls that column smoothly. No directional sliding of the left bars; original bar reveal remains.
- First invitation pointer starts 500 ms after the first badge reveal completes. Navigating before then dismisses it.
- Buttons use the existing icon set at 16 px, with 36 px icon buttons. Footer actions are right aligned.
- Complete lists open in a modal. Own municipality floats at the bottom while outside the viewport; clicking it scrolls to the real row and hides the duplicate.
- Category geometry is owned by `public/atlas-design-preview/ranking.css`; conflicting legacy selectors were removed from the three older preview stylesheets.

## Data boundaries

- 27 saved discovery lists have complete rows; 12 support rank comparison since the start of the year. The three initial district metrics are separate.
- `fetch-ranking-data.ts` reads the central complete observations. Last central observation found: 2026-09-09, no full prior-month observation. A newer verified local source from 2026-09-18 is retained rather than downgraded.
- Missing historical comparisons remain `—`, never zero. Batteries per 100 private rooftop systems is a ratio of two independently counted stocks, not the percentage of roofs with a battery.
- The data-fetch script currently references local source/cache paths; production integration must use the application's authoritative data pipeline.

## Verification and remaining work

- Browser checks at 708 px and 390 px: compact headings, list overflow, fast forward/back navigation, consistent stage height, modal layout, floating own row, share/copy actions.
- Latest right-column scrolling correction checked in browser; pointer delay is syntax-checked but its full timing sequence has not been remeasured.
- Ranking comparison tests and dashboard tests are available under `scripts/municipality-preview` and `lib/dashboard`.
- An existing MutationObserver initialization error was observed in the broader preview; its source is not yet isolated.
- Production integration, full production build, release review and deployment remain outstanding.

## Type-check boundary

The local `scripts/municipality-preview` adapter is excluded from the production TypeScript project because its esbuild aliases resolve into external design workspaces. Application components and `lib/dashboard` remain in the normal strict production check. The preview client is separately bundled with the same aliases as its server; its focused data tests are run separately. Portability and strict typing of that adapter remain integration work.
